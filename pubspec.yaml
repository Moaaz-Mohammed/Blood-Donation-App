name: blood_donation
description: An app to connect volunteers and patients

publish_to: 'none' # Remove this line if you wish to publish to pub.dev
version: 1.0.0+2

environment:
  sdk: ">=2.12.0 <3.0.0"

dependencies:
  flutter:
    sdk: flutter

  #Styles
  fluttertoast:
  cupertino_icons: ^1.0.2
  page_transition:
  image_picker:
  cached_network_image:
  smooth_page_indicator:
  im_stepper:
  flutter_switch:
  flutter_svg:


  # Utilities
  shared_preferences:
  url_launcher:
  flutter_bloc:
  modal_progress_hud_nsn:

  #Localization
  easy_localization:

  # firebase dependencies
  firebase_core:
  cloud_firestore:
  firebase_storage:
  firebase_auth:

dev_dependencies:
  flutter_test:
    sdk: flutter

  flutter_lints: ^1.0.0

flutter:

  uses-material-design: true

  assets:
    - assets/images/